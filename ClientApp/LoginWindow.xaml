<Window x:Class="ClientApp.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="180" Width="300">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <Label Width="80" Content="Username:"/>
            <TextBox x:Name="UsernameBox" Width="180"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,10">
            <Label Width="80" Content="Password:"/>
            <PasswordBox x:Name="PasswordBox" Width="180"/>
        </StackPanel>
        <Button Grid.Row="2" Content="Login" Width="80" HorizontalAlignment="Right" Click="Login_Click"/>
    </Grid>
</Window>
